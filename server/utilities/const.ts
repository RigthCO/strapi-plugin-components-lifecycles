/** Current plugin UID */
export const PLUGIN = `plugin::strapi-plugin-components-lifecycles`

/** Current plugin configurations */
export const CONFIG = {
    /** Stores the map'd components lifecycles */
    LIFECYCLES: `${PLUGIN}.lifecycles`
}
